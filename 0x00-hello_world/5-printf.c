#include <stdio.h>
/* 
 * main - A program that prints output followed by a newline
 * Return: 0
 */
int main (void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
